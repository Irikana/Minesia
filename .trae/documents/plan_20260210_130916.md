# 问题分析

## 错误原因
- **错误类型**：类型错误 (`TypeError: not a function`)
- **错误位置**：`updatePlayerAttributes` 函数中
- **可能原因**：
  1. `healthComponent` 不是预期的对象类型
  2. `setCurrentValue` 或 `setMaxValue` 方法不存在
  3. Minecraft Bedrock Edition API 中健康组件的方法名不同

## 当前情况
- 代码中尝试调用 `healthComponent.setCurrentValue(totalMaxHealth)`
- 代码中尝试调用 `healthComponent.setMaxValue(totalMaxHealth)`
- 这些方法可能在当前版本的 API 中不存在

## 解决方案

### 1. 修复 `updatePlayerAttributes` 函数
- 检查 Minecraft Bedrock Edition API 中健康组件的正确方法
- 使用正确的 API 方法设置生命值
- 确保代码与当前 API 版本兼容

### 2. 实现步骤
1. **修改 `actions.js` 文件**：
   - 移除不存在的方法调用
   - 使用正确的 API 方法（如果存在）
   - 或者使用命令方式修改生命值

2. **验证修复**：
   - 检查代码是否正确编译
   - 确保系统能够正常运行
   - 测试装备组合时的效果