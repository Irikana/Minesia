# 重构脚本文件夹结构并修复灵魂系统

## 1. 文件夹结构重构

### 创建文件夹
- `BP/scripts/soul/` - 存放灵魂系统相关文件
- `BP/scripts/equipment/` - 存放套装效果系统相关文件

### 移动文件
- 将 `soulSystem.js` 和 `soulHud.js` 移动到 `BP/scripts/soul/`
- 将 `rules.js`、`equipment.js` 和 `actions.js` 移动到 `BP/scripts/equipment/`
- 保持 `main.js` 和 `commands.js` 在根脚本目录

## 2. 更新导入路径

### 更新 main.js
- 更新所有模块的导入路径以反映新的文件夹结构

### 更新其他文件
- 更新 `commands.js` 中的灵魂系统导入路径
- 确保所有文件的相对导入路径正确

## 3. 修复灵魂HUD不显示问题

### 检查分数板系统
- 确保分数板 `soul` 正确创建
- 修复分数板命令执行问题

### 优化HUD更新逻辑
- 改进 `soulHud.js` 中的HUD更新机制
- 添加更详细的调试信息

### 验证HUD配置
- 确保 `hud_screen.json` 中的 `query.score.soul` 配置正确
- 验证纹理文件路径正确

## 4. 修复命令系统

### 修复命令处理
- 确保 `commands.js` 中的命令能够正确执行
- 修复 `!soul` 命令的实现

### 添加命令测试
- 添加更全面的命令测试功能
- 确保测试命令能够正确执行

## 5. 系统集成测试

### 初始化测试
- 确保所有系统正确初始化
- 检查控制台日志中的初始化信息

### 功能测试
- 测试灵魂值的设置和修改
- 测试套装效果的应用
- 测试HUD的显示和更新

### 错误处理
- 确保系统在各种情况下都能稳定运行
- 添加适当的错误处理和日志记录

## 6. 最终验证

- 运行完整的系统测试
- 确保所有功能正常工作
- 提供详细的使用说明