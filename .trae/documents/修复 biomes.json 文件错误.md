## 修复 biomes.json 文件错误

### 问题分析
1. **JSON格式错误**：第173行有多余的 `}`，导致文件无法正确解析
2. **结构问题**：多个 `"minecraft:biome"` 对象直接作为根属性，这不符合正确的JSON结构
3. **内容问题**：海洋生物群系的表面参数设置不正确（使用了草方块和泥土）

### 修复方案
1. **移除多余的大括号**：删除第173行的多余 `}`
2. **重构文件结构**：将所有 `"minecraft:biome"` 对象组织到一个数组中，正确的结构应该是：
   ```json
   {
     "format_version": "1.16.0",
     "minecraft:biomes": [
       { /* 平原生物群系 */ },
       { /* 森林生物群系 */ },
       { /* 沙漠生物群系 */ },
       { /* 山脉生物群系 */ },
       { /* 海洋生物群系 */ }
     ]
   }
   ```
3. **修正海洋生物群系参数**：更新海洋生物群系的表面参数，使用更合适的材料

### 具体修改步骤
1. 读取完整的 biomes.json 文件内容
2. 移除第173行的多余 `}`
3. 将根级别的多个 `"minecraft:biome"` 属性重构成一个 `"minecraft:biomes"` 数组
4. 更新海洋生物群系的表面参数
5. 验证修复后的JSON格式是否正确